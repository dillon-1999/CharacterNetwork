<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Friends</title>
    <link rel="stylesheet" type="text/css" href="/styles/styles.css">
</head>
<style>
</style>
<body>
    <% if(session.isLoggedIn){ %>
        <a href="../users/homepage?userID=<%=session.userID%>"><div class='text-center color1' id="header">
            <h1>Character Network</h1>
            <br>
        </div></a>
        <div>
            <% if(friends.length === 0){ %>
                    <h2 class="color3 text-center"> (｡•́︿•̀｡) <br> No friends to display!</h3>
                    <a href="../users/homepage?userID=<%=session.userID%>"><button class="color3 block-center">Go Home</button></a>
                <% } %>
            <% for(let i = 0; i < friends.length; ++i){ %>
                <div class="box">
                    <a class="bigger-text" href="../users/homepage?userID=<%= friends[i].friendID %>"><%= friends[i].username %></a> <br><br>
                    <p>Date Friended: <%= friends[i].dateFriended %></p>
                    <form action="/users/unfriendUser?friendID=<%= friends[i].friendID %>" method="post">
                        <button type="submit" class="fancy-button2 clickable">Unfriend User</button>
                    </form>
                </div>
            <% } %>
        </div>
    <% } else { %>
        <%- include('partials/accessDenied.ejs') %>
    <% } %>
</body>
</html>
